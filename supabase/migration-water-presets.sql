-- =====================================================================
-- Nucleus -- Трекер воды: кастомные порции быстрого добавления.
-- Храним список порций (мл) в app_settings.water_presets (JSON-массив чисел),
-- чтобы пользователь мог добавлять/удалять свои значения, и они
-- синхронизировались между устройствами. NULL = список по умолчанию.
-- Выполнить ОДИН раз в Supabase (SQL Editor). Безопасно повторно.
-- =====================================================================

alter table public.app_settings
  add column if not exists water_presets jsonb;
